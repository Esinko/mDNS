/**
 * @typedef {String} IPResolvable String that is standard IPv4 format
 */

/**
 * @typedef {String} mDNSAddress Standard mDNS address ending with .local
 */

/**
 * @typedef {String} DNSRecordType DNS record type. (Only A supported)
 */